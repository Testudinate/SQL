Операторы в PL\SQL

--логические                          }
--арифметические                      } Как в SQL
--конкатенации                        }
--возведения в степень(**)
--скобки                                Как в SQL

v_count := v_count + 1;
v_equal := (v_n1 = v_n2);
v_valid := (v_emp_id IS NOT NULL);

